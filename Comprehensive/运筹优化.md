---
aka description: 最优化问题（Optimization problem）
---
## 一、简述
最优化问题在数学与计算机科学领域中，**从所有可行解中寻找最优良的解的问题**。根据变数是连续的或离散的，最佳化问题可分为两类：连续最佳化问题与组合优化。

相对于决策问题 (Decision problem)、功能性问题 (Function problem)，最优化问题是：从问题的多个解中，求出最佳解。例子：背包问题。

运筹学的具体内容包括：规划论（包括线性规划、非线性规划、整数规划和动态规划）、库存论、图论、决策论、对策论、排队论、可靠性理论等。

## 二、建模

求解优化模型的算法，大致可以分为两类，**启发式算法和精确解算法**。

### MIP 问题

MIP问题，即混合整数规划问题(Mixed integer programming)。MIP的求解方法可以分为：精确求解和近似求解两大类。

#### 精确求解

精确求解的目标是求得**最优解 (global optimal solution)**，但作为代价求解的过程需要花很长时间 (exponential time)，所以更适合于规模比较小的 MIP 问题。

精确解算法，通常用于求解混合整数规划模型。从最简单的分支定界，割平面，到列生成，benders分解，拉格朗日松弛，到最难的分支定价。除了分支定界和割平面有比较通用的算法框架外，其他高阶一些的精确解算法，完全需要根据问题特点来构建模型分解模型，与其说是算法，不如说是建模技巧更合适。人们通常说精确解算法比启发式算法要难，这种难不是难在编程，而是难在建模和数学推导。大厂招聘运筹优化算法工程师，一般把启发式算法当做基础能力，精确解算法作为加分项。


#### 近似求解

近似求解并不要求求得最优解，而是希望能在**短时间**内求得**较优解** (suboptimal solution)

- $\epsilon$ -approximation：$\epsilon$ -近似算法要求在短时间内 (in polynomial time) 求得**次优解**(suboptimal solution)， 并且可以**保证**解的优秀程度 (bound on the degree of suboptimality)。

- metaheuristics：启发式算法往往能在短时间内 (empirically fast) 获得**较优解**(suboptimal solution)，但**不能保证**解的优秀程度 (without a gurantee of its quality)。由于启发式算法通常求解速度快，适用于规模中等或规模大的 MIP 问题，所以企业中常常使用启发式算法求解问题。


大体上看，启发式算法主要分两类，一类以邻域搜索为中心，比如模拟退火，禁忌搜索，迭代局部搜索，变邻域搜索，自适应大邻域搜索等算法。另一类是群智能算法，比如遗传算法，蚁群算法，粒子群算法等。启发式算法，说白了就是有方向的穷举法，在计算资源有限的情况下，算法工程师需要根据问题场景和模型特点，选择合理的邻域结构或操作机制，在全局搜索能力和局部搜索能力之间做权衡。


### 连续最优化问题

连续最优化问题的**标准的形式化**是（变量是连续的）
$$
\begin{array}{ll}
\underset{x}{\operatorname{minimize}} & f(x) \\
\text { subject to } & g_i(x) \leq 0, \quad i=1, \ldots, m \\
& h_j(x)=0, \quad j=1, \ldots, p
\end{array}
$$
这里 $f(x)$ 是目标函数, 通过 n 个 x 来最小化 f
g (x) 是不等式约束条件；m>=0
h (x) 是等式约束条件； p>=0.

如果 m 和 p = 0，则问题是一个**无约束的优化问题**。

经典算法


## 三、供应链

研发 — 计划 — 采购 — 生产 — 运输 — 售后

**四大场景**
- 仓网规划
- 智能补货
- 运输智能调度
- 工业制造

**目标**：
- 降低成本、降低时间
- 降本增效：

**流程**：
- 从业务场景中抽象为数学模型（决策变量、目标、约束）
- 根据数学模型求解优化问题

**强化学习怎么解决**：
路径优化距离：

强化学习有三要素：状态、动作、奖励。
如何设计状态、动作；
如何合理的把数据抽象为状态-动作对，输入状态可以是已有的信息（各个目标点，自己当前所在位置，周围环境的信息，或上一时刻的动作，序列决策），输出动作就是超哪个点进行移动。 然后就是设立奖励函数，这个奖励函数就与我们的目标是有关系的（目标点的距离的奖励，现在所花步数的惩罚；降本增效），
框架有了之后， 根据状态、动作类型，然后可以利用在线强化学习、离线强化学习进行决策，通过不断学习试错，达到最高的回报值。

当前也可以根据历史已有数据（专家数据）结合模仿学习和强化学习共同进行一个决策。



![[OP.png]]


- [路径规划之 A* 算法](https://zhuanlan.zhihu.com/p/54510444)



## 参考：
- [运筹优化（一）--运筹学概述](https://blog.csdn.net/App_12062011/article/details/86035587)
- [MIP求解方法总结](https://codeantenna.com/a/YWvfhEizlV)
- [运筹优化算法工程师的从业情况和能力要求](https://zhuanlan.zhihu.com/p/83648122)
