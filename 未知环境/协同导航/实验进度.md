


修改包名

https://www.guyuehome.com/35250





shared_collect_rollout 函数去掉 episode
需要修改插入 buffer 的元素个数，加入 next_obs


用于判断任务是否完成（每个智能体到达不同的目标点）

根据所选目标点：

`navigation`
- 获取每个机器人当前的状态
- 输入到训练好的神经网络中，得到每个智能体的动作（线速度、角速度）
- 判断智能体是否到达相应的目标点，与目标点的距离小于设定值，速度为0
- 每个智能体执行当前动作 3s
- 然后使 move_base 为 0




修改相对目标位置

定位问题