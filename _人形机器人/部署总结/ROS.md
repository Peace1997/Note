

odom：里程计信息，记录机器人相对于一个短期参考坐标系（通常在启动时固定）的位姿与速度估计，并随时间累积误差。
作用：主要是给控制器和局部运动规划提供连续的、频繁更新的位姿/速度信息

tf：用于发布与查询不同坐标系之间的变换，形成一棵以某一帧为根的帧数。包括静态变换和动态变换。动态变换是随机遍变换例如`odom->base_link`；静态变换是始终不变的，比如`base_link`与某个固定的传感器的相对位姿。
作用：在不同传感器、控制器与可视化模块之间共享坐标变换，